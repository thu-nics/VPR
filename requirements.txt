# requirements.txt records the full set of dependencies for development
accelerate
codetiming
datasets
dill
flash-attn
hydra-core
liger-kernel
numpy
pandas
peft
pyarrow>=19.0.0
pybind11
pylatexenc
pre-commit
ray[default]<=2.57.0,>=2.57.0
tensordict>=0.8.0,<=0.10.0,!=0.9.0
torchdata
transformers<=4.57.3
# Install vLLM through `pip install -e ".[vllm]"` so its Torch-compatible
# wheel can be selected for the target CUDA platform.
wandb
packaging>=20.0
uvicorn
fastapi
qwen-vl-utils[decord]
